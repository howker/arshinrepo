services:
  app:
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
  nginx:
    depends_on:
      app:
        condition: service_healthy
